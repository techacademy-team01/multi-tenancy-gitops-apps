apiVersion: appconnect.ibm.com/v1beta1
kind: Configuration
metadata:
  name: ${POLICY_NAME}
  namespace: ${NS}
spec:
  type: barauth 
  description: Authentication for GitHub
  data: ${BARAUTH_DATA}
  